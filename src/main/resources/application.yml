spring:
  application:
    name: daybrew-be
  task:
    scheduling:
      pool:
        size: 2
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/daybrew}
    username: ${DB_USERNAME:daybrew}
    password: ${DB_PASSWORD:daybrew}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.daybrew: DEBUG
    org.flywaydb: INFO

daybrew:
  jwt:
    secret: ${JWT_SECRET:daybrew-secret-key-change-in-production-32ch}
    expiration-ms: 86400000
  admin:
    username: ${ADMIN_USERNAME:admin}
    password: ${ADMIN_PASSWORD:}
  slack:
    webhook-url: ${SLACK_WEBHOOK_URL:}
  gemini:
    api-key: ${GEMINI_API_KEY:}
    base-url: https://generativelanguage.googleapis.com
    model: ${GEMINI_MODEL:gemini-2.5-flash-lite}
  pipeline:
    reddit:
      subreddits: SaaS,entrepreneur,startups,webdev,programming
      user-agent: daybrew/1.0 (idea-curator)
    github:
      pat: ${GITHUB_PAT:}
      min-stars: 100
      max-stars: 2000
    viral:
      seed-topics:
        - browser-extension
        - cli-tool
        - discord-bot
        - notion-integration
        - chrome-extension
